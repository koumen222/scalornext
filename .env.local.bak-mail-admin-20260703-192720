# ── Dev local : backend Express local (patché i18n, non déployé) — PORT=3001 (Backend/.env) ──
NEXT_PUBLIC_API_URL=http://localhost:3001
NEXT_PUBLIC_BACKEND_URL=http://localhost:3001

# ── Pour repointer sur la prod, commente les 2 lignes ci-dessus et décommente : ──
# NEXT_PUBLIC_API_URL=https://api.scalor.net
# NEXT_PUBLIC_BACKEND_URL=https://api.scalor.net
